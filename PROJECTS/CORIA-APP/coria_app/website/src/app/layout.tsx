// This file is required for the app directory.
// It just passes through to the locale-specific layout.

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}